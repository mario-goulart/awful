;;; awful.setup -*- scheme -*-

;; Compile the extension
(compile -s -O3 -lambda-lift -d1 -j awful awful.scm)
(compile -s -O3 -lambda-lift -d1 awful.import.scm)

;; Compile the application (server)
(compile -O3 -lambda-lift -d1 awful-server.scm -o awful)

(install-extension 'awful
                   '("awful.so" "awful.import.so")
                   '((version "0.19")))

(install-program 'awful
                 '("awful")
                 `((version "0.19")))
